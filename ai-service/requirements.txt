fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# PyTorch — CPU build (smaller download; remove +cpu for GPU)
torch>=2.2.0

# HuggingFace — sentiment + FLAN-T5 reply generation
transformers>=4.41.0
tokenizers>=0.19.0
sentencepiece>=0.2.0
accelerate>=0.28.0
datasets>=2.19.0

# Training utilities
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
tqdm>=4.66.0
regex>=2024.5.15

# Optional: Anthropic (tertiary fallback — only used if ANTHROPIC_API_KEY is set)
anthropic>=0.28.0